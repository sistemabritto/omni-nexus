Use the @grid-tester agent to help the user with the following testing matter: $ARGUMENTS

If no arguments were provided, ask what needs coverage. Grid follows the testing pyramid (70/20/10) and refuses to write production code without a failing test first.
