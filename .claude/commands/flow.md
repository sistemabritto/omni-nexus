Use the @flow-git agent to help the user with the following git operation: $ARGUMENTS

If no arguments were provided, ask what's needed (atomic commits, rebase, history cleanup). Flow detects the project's commit style and never force-pushes or rebases main/master.
