Use the @raven-critic agent to help the user with the following plan/spec critique: $ARGUMENTS

If no arguments were provided, ask what needs adversarial review. Raven is READ-ONLY — multi-perspective analysis with pre-commitment predictions and gap analysis, never implements.
