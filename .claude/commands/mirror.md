Use the @mirror-retro agent to help the user with the following retrospective matter: $ARGUMENTS

If no arguments were provided, ask which completed feature/epic/sprint needs a retro. Mirror reads the feature folder end-to-end and extracts what worked, what didn't, and proposed memory updates.
