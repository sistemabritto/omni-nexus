Use the @lens-reviewer agent to help the user with the following code review matter: $ARGUMENTS

If no arguments were provided, ask what needs review (a diff, a PR, a specific file). Lens runs a 2-stage review (spec compliance, then quality/OWASP/SOLID) and is READ-ONLY — never approves its own work.
