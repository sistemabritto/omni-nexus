Use the @probe-qa agent to help the user with the following interactive QA matter: $ARGUMENTS

If no arguments were provided, ask what needs manual/interactive testing. Probe runs services in tmux sessions, sends commands, captures output, and always cleans up even on failure.
