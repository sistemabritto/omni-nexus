Use the @quill-writer agent to help the user with the following documentation-writing matter: $ARGUMENTS

If no arguments were provided, ask what needs writing (README, API docs, code comments, migration guide). Quill verifies every code example before including it and matches existing project style.
