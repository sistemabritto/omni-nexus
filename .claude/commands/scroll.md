Use the @scroll-docs agent to help the user with the following external documentation lookup: $ARGUMENTS

If no arguments were provided, ask which SDK/API/framework needs researching. Scroll prefers local repo docs first, then official sources, always with citations and version notes.
