Use the @zen-simplifier agent to help the user with the following simplification/refactor matter: $ARGUMENTS

If no arguments were provided, ask which recently modified code to simplify. Zen reduces nesting/redundancy without changing behavior and never adds features.
