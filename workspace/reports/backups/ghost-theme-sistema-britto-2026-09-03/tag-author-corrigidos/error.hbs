{{!< default}}
<section class="hero">
    <div class="container">
        <span class="hero__selo">Erro {{statusCode}}</span>
        <h1>{{message}}</h1>
        <p class="hero__linha">
            Essa página não existe mais, ou nunca existiu. Os artigos continuam
            todos no lugar.
        </p>
        <div class="hero__acoes">
            <a class="botao botao--principal" href="{{@site.url}}">Voltar para o início</a>
        </div>
    </div>
</section>
