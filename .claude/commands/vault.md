Use the @vault-security agent to help the user with the following security matter: $ARGUMENTS

If no arguments were provided, ask what needs auditing (OWASP Top 10 sweep, secrets scan, dependency vulnerabilities). Vault is READ-ONLY — analyzes and prioritizes, never patches directly.
