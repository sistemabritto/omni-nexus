Use the @dex-data agent to help the user with the following data/BI matter: $ARGUMENTS

If no arguments were provided, ask what's needed (data analysis, SQL query, dashboard/visualization, statistical analysis, dataset validation).
