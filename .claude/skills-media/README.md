# skills-media

Scoped skill catalog for the media-worker's OpenCode process (ADR-6 in
workspace/development/features/social-media-production/). Populated by
scripts/update_hyperframes_skills.sh — never the full 199-skill
`.claude/skills/` catalog (that's what made the native `skill` tool too
expensive to leave on for the main dashboard/scheduler containers).
