Use the @scout-explorer agent to help the user with the following codebase-search matter: $ARGUMENTS

If no arguments were provided, ask what needs finding. Scout runs fast parallel searches (Haiku) and returns absolute file:line paths with relationships explained — READ-ONLY.
