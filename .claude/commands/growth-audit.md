Use the shared `growth-audit` skill to perform a read-only, evidence-backed business audit for the requested period: $ARGUMENTS

Default to the last 30 days when no period is supplied. Do not publish, message leads, schedule content, create tickets, mutate production, or expose personal data/secrets. Route editorial implications to Pixel and commercial queue implications to the existing CRM owner; do not create another social-media agent.
