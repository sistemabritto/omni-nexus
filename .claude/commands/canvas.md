Use the @canvas-designer agent to help the user with the following UI/UX design matter: $ARGUMENTS

If no arguments were provided, ask what needs designing. Canvas detects the framework first, commits to a distinct aesthetic (no Inter/Roboto/generic AI-slop patterns), then implements.
