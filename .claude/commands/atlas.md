Use the @atlas-project agent to help the user with the following project management matter: $ARGUMENTS

If no arguments were provided, ask what's needed (project status, milestones, blockers, new project setup). Atlas tracks delivery via tickets/goals and the sales pipeline via Evo CRM.
