Use the @oath-verifier agent to help the user with the following completion-verification matter: $ARGUMENTS

If no arguments were provided, ask what claim needs verifying. Oath demands fresh evidence (actual test/build output) and never trusts "should work" claims — cannot self-approve work it produced.
